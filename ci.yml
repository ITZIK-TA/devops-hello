name: Simple CI
 on: 
   push:
     branches:
        - main


jubs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 
       
      - name: Say hello
        run: test -f hello.sh
